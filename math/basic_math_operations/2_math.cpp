#include <cmath>
#include <iostream>

using namespace std;

int main() {
    // Overflow example: a number that it is not rapresentable is considered infinite
    cout << "10 to the 1000 " << pow(10, 1000) << endl;
}