#include <iostudio> 
#include <cmath>

using namespace std;

int main() {
    // dichiarazione
    double a, b;

    assegnazione
    a = 2.0;
    b = 8.0;

    // Calcolo di c = log_a(b)
    double c = log(b) / log(a)

    cout << "C = log_a(b) -> C = " << c << endl;
    return 0;
}