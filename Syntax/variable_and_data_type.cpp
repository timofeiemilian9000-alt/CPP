#include <iostream>

using namespace std;

int main() {
    /*  
        qui ho imparato ad usare ctrl + k + u per togliere i commenti
        ctrl + k + c per metterli 
        in più righe
    */
    int età = 20;
    float altezza = 1.75;
    double prezzo = 19.99;
    char lettera = 'A';
    bool accesso = true;
    string nome = "Marco";
    // cout << nome;
    // return 0;
    /*
        ho imparato come compilare manualmente da terminale con g++ -o <nome del file che interpreta il computer> <nome del file>
        ho aggiunto 2 errori alla mia collezione 
    */
}